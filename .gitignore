node_modules
dist
dist-ssr
*.local
# Secrets: encryption keys, pepper, passwords (see .env.example)
.env
.DS_Store
.vite
dev-dist
.claude/
# Runtime content store written by server/index.js (local, non-Docker runs)
data-store/
